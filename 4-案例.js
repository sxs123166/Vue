
// 创建列表隔行变色项目
// 1）新建项目空白目录，并运行npm init -y命令，初始化包配置管理文件package.json
// 2)新建src源代码目录
// 3）新建src-> index.html 首页和src->index.js文件
// 4）初始化首页基本的结构
// 5）运行npm install jquery -S命令，安装jQuery
// 6) 通过ES6模块化的方式导入jQuery, 实现列表隔行变色效果

// dependencies下的节点 开发和上线时都会用到，将版本号加到package下的此节点中的命令为：
// npm install 包名@版本号 -S

// devDependencies 只在开发阶段会用到，上线时不需要，将版本号加到package下的此节点中的命令为：
// npm install 包名@版本号 -D

// -s 是--save的简写
// -D 是 --save-dev的简写


